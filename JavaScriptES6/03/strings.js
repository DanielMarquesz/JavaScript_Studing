
// Tamanho de uma string
const textSize = "Rato roeu"
console.log(textSize.length)

// Retorna um array coma string separada por determinado delimitador
const sliceString = "Abacaxi"
console.log(sliceString.split("a"))

// Busca por um determinado caracter e substitui o mesmo
const replaString = "Abacate"
console.log(replaString.replace("e","P"))

// Retorna a fatia de uma string apartir de determinada posição
const lastChar = "Texto".slice(-4)
console.log(lastChar)

// Retorna tudo entre duas posições de uma string
const muitoChar = "Daniel".slice(0,2)
console.log(muitoChar)